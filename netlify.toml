[build]
PHP_VERSION=7.2

command = "source /opt/buildhome/.phpbrew/bashrc && phpbrew install 7.2 +default && phpbrew switch 7.1.3 && phpbrew app get composer && composer install --optimize-autoloader --no-dev && php artisan config:cache && php artisan route:cache"
publish = "public"